{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "curl -fsSL https://bun.sh/install | bash && export PATH=\"$HOME/.bun/bin:$PATH\" && cd duo && bun install",
  "buildCommand": "curl -fsSL https://bun.sh/install | bash && export PATH=\"$HOME/.bun/bin:$PATH\" && cd duo && python3 -m venv .vercel-py && .vercel-py/bin/pip install usd-core && .vercel-py/bin/python scripts/prepare-model.py && bun run build",
  "outputDirectory": "duo/dist"
}
